import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.cari.stlaurent",
  appName: "CARI St-Laurent",
  webDir: "build",
  server: {
    androidScheme: "https",
  },
};

export default config;
